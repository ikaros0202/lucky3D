# Lucky3D 项目协作规则

## 作用域

- 本文件适用于整个仓库。
- 如果以后某个子目录需要不同规则，应在该子目录增加更具体的 `AGENTS.md` 或 `AGENTS.override.md`，不要把局部规则继续堆到根文件。
- 当前阶段和下一项工作以 `tasks/todo.md` 为准，不要把容易过期的进度状态写死在本文件中。

## 开始工作前

按以下顺序读取与任务相关的资料：

1. `AGENTS.md`
2. `PRD-lucky3D.md`
3. `docs/data-source-and-sync.md`
4. `docs/calculation-rules.md`
5. `docs/superpowers/specs/2026-07-28-lucky3d-predevelopment-design.md`
6. `tasks/plan.md`
7. `tasks/todo.md`

涉及 UI、交互或图表时，还要读取 `docs/ui-design-skill-research.md`。只读取与当前任务有关的章节和文件；如果用户点名了文件，先从点名文件开始。

发生冲突时：

- 用户当前明确指示优先于项目文档。
- 产品范围以 `PRD-lucky3D.md` 为准。
- 数据来源与同步行为以 `docs/data-source-and-sync.md` 为准。
- 指标、筛选、回测和金额公式以 `docs/calculation-rules.md` 为准。
- 技术结构以开发前技术设计为准。
- 执行顺序和当前状态以 `tasks/plan.md`、`tasks/todo.md` 为准。
- 已确认决策发生变化时，同步更新受影响的文档，避免代码与文档分叉。

## 产品定位与边界

Lucky3D 是只服务于中国福利彩票“3D”玩法的 Android 本地数据分析工具。核心流程是：

```text
查看开奖 → 观察走势 → 设置筛选条件 → 保存方案 → 开奖后复盘
```

首版必须遵守：

- 只支持福彩3D，以及单选、组选3、组选6三种玩法。
- 一级入口固定为首页、走势、选号、方案、彩报。
- 不建设云端服务，也不把开发者个人电脑作为后端。
- 不提供账号、跨设备同步、在线购彩、支付、充值、代购或兑奖。
- 不提供 AI 荐号、专家收费方案、“必中”承诺或未来中奖概率。
- 不提供社区、评论、私信、导出、分享或文件备份。
- V1 已完成的静态彩报占位页保留为历史记录；V1.2 按已确认公开页面适配器展示 55125.cn 福彩 3D 试机号和牛彩网 A11 彩吧彩报第三版。两者均不是官方开奖结果；试机号内置 2025 全年及 2026 打包日前历史基线并在应用前台更新，彩报支持最近 30 个自然日内换期，但不提供收藏、分享、下载、购买或第三方分析文章聚合。
- V1.1 不把第三方彩报图片提交到 Git 或打包进 APK；公开发布完整图片展示前必须确认来源展示授权。
- 不扩展排列3、双色球或其他彩种。
- 界面文案保持“数据分析工具”定位，不使用赌场、诱导投注、夸大收益或制造焦虑的表达。

## 技术约束

- 语言：Kotlin。
- UI：Jetpack Compose + Material 3。
- 架构：单 Activity、单向数据流、不可变 `UiState`。
- 首版工程：单 `app` 模块，通过包边界分层。
- 导航：Navigation 3。
- 本地数据库：Room。
- 网络：OkHttp。
- JSON：kotlinx.serialization。
- 并发与状态：Coroutines、Flow、StateFlow。
- 依赖注入：Hilt。
- 小型设置：Preferences DataStore。
- Java 工具链：JDK 17。
- `minSdk 26`，`compileSdk 37`，`targetSdk 37`；调整前必须核对官方文档和项目计划。
- V1 不引入 WorkManager，只在应用启动、进入前台或用户手动刷新时同步。
- 依赖必须使用明确的稳定版本，禁止动态版本。

分层约束：

- Room 是运行时唯一事实来源；网络响应必须校验并写入 Room 后才能影响界面。
- `feature` 不直接访问 DAO 或网络 DTO，只依赖 Repository、Use Case 和领域模型。
- `domain` 计算保持为无 Android 依赖的纯 Kotlin。
- 网络 DTO、Room Entity、领域模型和界面模型分别定义，不共享可变对象。
- 跨层错误使用明确类型表示，不用异常文本驱动业务状态。

## 数据与计算正确性

- 安装包内置从 `2017001` 期开始的开奖基线；2017 年以前的数据不进入 V1。
- 官方福利彩票公开查询接口是增量同步来源，第三方来源只能用于独立抽样审计。
- 网络数据必须整批完成结构、期号、日期、号码范围、重复和连续性校验；失败时不得部分写库。
- 新增、修正和同步元数据必须在单个 Room 事务中合并。
- 官方修正历史数据后，必须重算受影响的指标、回测和方案复盘。
- 同一数据版本、规则版本和筛选条件必须产生完全相同的结果。
- 三位号码必须保留前导零，例如 `007` 不能变成 `7`。
- 回测目标期只能读取该期之前可获得的数据，禁止未来数据泄漏。
- 页面不得根据第三方文章、空响应或异常载荷推断正式开奖结果。
- 数据源失败时保留本地数据，显示最后成功更新时间、错误状态和重试入口。

修改种子数据、校验脚本或计算规则时，必须使用项目现有黄金样例和验证脚本重新验证；不得仅凭界面结果判断正确。

## 开发与验证

- 开始实现前检查 `tasks/todo.md`，只推进已批准的阶段和任务。
- 领域逻辑、筛选、同步、数据库迁移和缺陷修复先写失败测试，再写最小实现。
- 新依赖、新 Android API 和行为敏感实现必须以官方文档为依据。
- 保留用户已有修改，不覆盖无关文件，不使用破坏性数据库迁移代替正确迁移。
- 不为当前需求顺带扩展功能、彩种、后端或权限。
- 用户要求诊断或评审时，只调查和报告；只有用户同时要求修改时才实施。
- 改变用户可见行为、数据契约或架构决策时，同步更新相关 PRD、设计、任务或 ADR。

Android 工程创建后，至少使用 Gradle Wrapper 运行与修改范围相称的检查：

```powershell
.\gradlew test
.\gradlew lint
.\gradlew assembleDebug
```

发布前还要完成相关 UI/端到端测试、Release 构建和真机或模拟器主流程验收。数据相关改动应同时运行：

```powershell
powershell -ExecutionPolicy Bypass -File tools/validate-predevelopment-data.ps1
```

只有在命令本轮实际执行成功后，才能声称构建、测试或验收通过。交付说明必须列出修改文件、验证命令、结果和仍存在的风险。

## UI 设计原则

- 产品气质是可信、克制的专业数据工具，不使用赌场式霓虹、金币、中奖庆祝或诱导性视觉。
- 页面只展示用户完成当前任务所需的信息，不展示数据抓取来源、检查时间、缓存机制、内部边界或其他面向开发者的项目背景；试机号区域只使用“试机号”标题和必要的用户状态。
- 正常状态必须按已确认设计图的比例在一个手机屏幕内展示全部核心内容，不得把确认稿拆成需要连续滚动的大组件长页。
- 先解决信息层级和任务流，再选择颜色、阴影和装饰。
- 首页、走势、选号、方案、彩报必须共享同一套颜色、字体、间距、圆角、图标和状态语义。
- 每个页面必须定义内容、加载、空、错误、无网和重试状态；涉及保存或同步时还要定义成功状态。
- 走势图、遗漏、冷热和命中状态不得只依赖红绿或单一颜色，必须同时使用数字、标签、形状或线型。
- 使用真实历史数据验证密度、滚动、缩放、截断和极端情况，不用理想化占位数据代替。
- 触控目标按 Android 规范至少为 `48dp`。
- 本项目不把无障碍模式或系统字体缩放作为设计、实现和验收范围；仍需覆盖基准手机、深浅色模式、前导零和真实长文本。
- 不在界面使用“预测”“推荐号码”“必中”“提高中奖率”等无法证明的措辞。

## 五个 UI 技能的使用规则

只使用与当前任务直接相关的最小技能集合。使用技能前读取其完整 `SKILL.md`，并在工作更新中简短说明使用了哪个技能以及原因。技能建议不能覆盖产品需求、用户决定或 Android 官方规范。

### `oiloil-ui-ux-guide`

以下情况使用：

- 为新项目或多个页面建立、重定向或统一设计系统。
- 设计首页、走势、选号、方案等新页面族。
- 需要生成或更新项目级 `design-spec.md`。
- 用户要求对现有界面、截图或实现做完整 UI/UX 评审。

新建设计默认使用 `design` 模式；只需要某类页面规则时使用 `guide`；评审现有界面时使用 `review`。单个颜色、按钮大小或一句文案等窄问题不启动完整咨询流程。

### `wireframe-spec`

以下情况使用：

- 新增页面、主流程或重要弹窗。
- 导航、信息架构、内容优先级或页面布局发生显著变化。
- 复杂筛选区、走势图控制区、方案详情等需要先确认空间关系。

在线框阶段只确定层级、位置、交互和状态，不提前决定视觉风格。线框必须覆盖加载、空、正常、错误和必要的窄屏状态。

### `ui-ux-pro-max`

以下情况使用：

- 选择或调整颜色、字体、间距、圆角、图标、动效和组件样式。
- 新建或重构 Compose 页面和可复用 UI 组件。
- 检查触控、导航、反馈、一致性和视觉完成度。

必须将平台明确限定为 Android 手机、技术栈明确限定为 `jetpack-compose`。新页面或新设计系统先查询完整设计系统，再按需查询具体领域；不得照搬 Web、GSAP、ARIA、CSS 或浏览器断点建议，应转换为 Material 3、Compose semantics、系统栏和 Android 窗口规则。

### `data-visualization`

以下情况必须使用：

- 走势图、遗漏、冷热、次数、分布、排行、回测或任何统计图表。
- 图表类型、坐标、图例、标签、缩放、滚动、触摸详情或颜色编码发生变化。
- 需要在图表和表格之间选择移动端呈现方式。

优先采用能表达任务的最简单图表，使用真实 `data/fc3d-seed.json` 数据验证。图表必须提供非颜色编码、清晰上下文和可访问的文字替代。

### `design-qa-checklist`

以下情况必须使用：

- UI 已实现，准备声明页面或 UI 任务完成。
- 设计稿、截图或 `design-spec.md` 与实现需要对比。
- UI 相关代码评审、里程碑验收和发布前检查。

验收至少覆盖视觉 token、确认稿比例、整屏信息、全部状态、真实内容、触控反馈、深浅色和目标模拟器。无障碍模式和字体缩放不属于本项目验收范围。

### 推荐组合

```text
新页面或重大改版：
oiloil-ui-ux-guide
  → wireframe-spec
  → ui-ux-pro-max
  → data-visualization（含图表时）
  → Compose 实现
  → design-qa-checklist

小范围视觉或交互调整：
ui-ux-pro-max
  → Compose 实现
  → design-qa-checklist

已有界面完整评审：
oiloil-ui-ux-guide（review）
  → data-visualization（含图表时）
  → design-qa-checklist
```

不得因为技能已经安装就无条件同时调用全部五个；纯数据、领域逻辑、数据库和网络任务不应触发 UI 设计技能。

## 代码评审规则

- 优先检查数据错误、未来数据泄漏、规则口径偏差、同步污染、破坏性迁移和用户数据丢失。
- 检查是否越过当前产品边界，尤其是云后端、在线购彩、AI 荐号、未经批准的第三方内容扩展和其他彩种。
- 检查 `feature`、`domain`、Repository、Room 和网络适配器之间的依赖方向。
- 检查新增行为是否有失败路径、边界测试和与风险相称的验证证据。
- UI 改动还要检查完整状态、真实数据密度、确认稿整屏比例和 `48dp` 触控目标。
- 只报告能由当前改动和代码证据支持的问题，给出精确文件位置、影响和安全修复路径。
- 格式化和机械风格问题交给自动化工具；人工评审聚焦正确性、架构、可维护性、确认稿还原和产品边界。
- 构建通过不等于功能正确；没有完成对应业务测试和真实流程验证时，不得批准为完成。

## 参考

- OpenAI Codex：[Custom instructions with AGENTS.md](https://developers.openai.com/codex/guides/agents-md)
- 本项目 UI 技能调研：`docs/ui-design-skill-research.md`
