# Lucky3D Android 设计规范

> 设计模式：`oiloil-ui-ux-guide` / `design`
> 风格家族：`modern-minimal`，针对中文、Android 手机和高密度历史数据做紧凑化调整
> 日期：2026-07-28
> 状态：Task 11—17 的实现基线

## 1. 设计方向

- **产品**：面向福彩3D历史数据使用者的本地 Android 分析工具，服务“查看—分析—筛选—保存—复盘”流程。
- **气质**：可信、克制、清晰；像随身数据仪表，不像彩票网站或博彩宣传页。
- **主语言**：简体中文 `zh-CN`；V1 不提供语言切换。
- **平台**：Android 手机竖屏，Jetpack Compose + Material 3，`minSdk 26`。
- **硬约束**：深浅色、系统字体放大、TalkBack、窄屏、`48dp` 触控目标、前导零、真实高密度数据。
- **视觉选择**：采用默认 A“深海蓝专业数据工具”；不使用霓虹、金币、彩带、中奖庆祝、焦虑或收益暗示。
- **验证说明**：本规范由已批准 PRD、技术设计和页面线框直接收敛；为连续完成全部待办，本轮跳过独立 HTML 业务稿确认，以实际 Compose 页面和模拟器截图作为最终视觉验证。

## 2. 颜色

所有实现使用 Material 3 语义角色，业务组件不直接散落十六进制颜色。

### 浅色

| 角色 | 值 | 用途 |
| --- | --- | --- |
| `primary` | `#1E4E79` | 主要操作、选中态、关键走势落点 |
| `onPrimary` | `#FFFFFF` | 主色上的文字和图标 |
| `primaryContainer` | `#D4E8FA` | 轻强调、选中筛选条件 |
| `onPrimaryContainer` | `#092F4F` | 主色容器内容 |
| `secondary` | `#526270` | 次要操作、辅助系列 |
| `secondaryContainer` | `#DCE7EF` | 次级状态容器 |
| `tertiary` | `#356E68` | 第三走势图系列、信息状态 |
| `background` | `#F6F8FB` | 页面背景 |
| `surface` | `#FBFCFE` | 主内容表面 |
| `surfaceVariant` | `#E4E8ED` | 分组、禁用、占位 |
| `outline` | `#727A83` | 边框、网格线 |
| `error` | `#BA1A1A` | 同步失败、无效输入 |
| `success` | `#276B48` | 已更新、已保存、已包含 |
| `warning` | `#8A5A00` | 待开奖、样本不足、数据修正 |

### 深色

| 角色 | 值 |
| --- | --- |
| `primary` | `#9CCBFA` |
| `onPrimary` | `#003353` |
| `primaryContainer` | `#164A72` |
| `onPrimaryContainer` | `#D0E7FF` |
| `secondary` | `#BAC8D5` |
| `secondaryContainer` | `#344955` |
| `tertiary` | `#9FD4CD` |
| `background` | `#0E1720` |
| `surface` | `#121C27` |
| `surfaceVariant` | `#404850` |
| `outline` | `#8A929B` |
| `error` | `#FFB4AB` |
| `success` | `#8ED7AE` |
| `warning` | `#F3C46D` |

### 状态编码

- 冷/温/热同时显示文字、次数和图形长度，颜色只作辅助。
- 命中/未命中同时显示“包含/未包含”文字和图标，不只使用绿/红。
- 同步状态始终包含状态文案和最后成功时间。
- 走势的百/十/个位使用“圆/方/菱形 + 百/十/个标签 + 线型”区分；颜色不是唯一编码。

## 3. 字体与数字

- 标题、正文：Android 系统无衬线字体；中文优先系统 `sans-serif`，不随 APK 打包网络字体。
- 期号、三位号码、金额和统计值：`FontFamily.Monospace` 或等宽数字特性，确保列对齐并保留前导零。
- 字重：正文 `400`，标签/按钮 `500`，页面标题和关键数字 `600`。
- Compose 类型层级：`labelSmall`、`labelMedium`、`bodySmall`、`bodyMedium`、`titleSmall`、`titleMedium`、`headlineSmall`、`headlineMedium`。
- 正文不小于 `14sp`；关键信息不依赖小于 `12sp` 的辅助文字。
- 中文正文行高约 `1.6`；标题不使用负字距。
- 字体放大到 `1.3x` 时允许卡片增高、文本换行和页面纵向滚动，不截断主要操作。

## 4. 间距

- 基础单位：`4dp`。
- 常用刻度：`4 / 8 / 12 / 16 / 20 / 24 / 32 / 48dp`。
- 页面水平边距：窄屏 `16dp`，常规手机 `20dp`。
- 页面主要分区间距：`24dp`；同组内容：`8—12dp`。
- 列表行最小高度：`56dp`；底部导航与所有可点击控件满足至少 `48dp`。
- 信息密度：平衡偏紧凑。走势图可横向滚动，但页面主体不能因固定宽度产生意外横向滚动。

## 5. 圆角

- 小：`8dp`，输入框、标签、状态块。
- 中：`12dp`，普通卡片、底部表单。
- 大：`16dp`，主数据卡、模态底部面板。
- 全圆：仅用于 FilterChip、状态徽标和开奖数字圆点。

## 6. 边界与层级

- 全局采用 `border + divider` 容器策略。
- 页面优先用留白、标题和分隔线分组；卡片只包裹真正独立的数据或操作单元。
- 卡片默认 `1dp` 低对比边框，无阴影。
- 对话框、底部面板可使用 Material 3 默认 elevation；普通列表不使用阴影。
- 禁止卡片嵌套卡片。需要次级分组时使用间距、底色或分隔线。

## 7. 图标、装饰与动效

### 图标

- 只使用 Material Symbols / Material Icons，轮廓与填充保持同一套语义。
- 常规尺寸 `20 / 24dp`；空状态 `40dp`。
- 图标继承文本或语义色，不混用 emoji。
- 图标按钮必须有 TalkBack 描述；非通用操作同时显示文字标签。

### 装饰

- 业务页不使用渐变、纹理、金币、彩带或赌博意象。
- 数据图表本身是唯一主要视觉动机。
- 彩报页仅展示已批准的静态占位照片及来源说明。

### 动效

- 词汇：minimal。
- 状态切换 `150—220ms` 淡入淡出；底部面板使用 Material 默认进入/退出。
- 允许：颜色/透明度变化、短距离位移、展开折叠。
- 禁止：弹跳、闪烁、转盘、庆祝粒子、循环吸引注意力的动画。

## 8. 通用组件

### 页面框架

- 顶部标题区承载页面名称、当前范围和全局操作。
- 底部五入口始终依次为：首页、走势、选号、方案、彩报。
- 一级页签切换保留各页主要状态；二级页面使用 Navigation 3 back stack。
- Android 返回键先关闭面板/详情，再回到来源页；根页面不在五个页签间反向遍历。

### 按钮

- 主按钮：填充主色，最小高度 `48dp`，文字说明动作。
- 次按钮：描边或 tonal；同一屏最多一个主按钮。
- 文本按钮：用于关闭、清除、查看详情等低风险操作。
- 破坏性操作需可撤销或确认；删除筛选条件后可撤销最后操作。

### 输入与筛选

- 标签常驻，不用 placeholder 代替字段名。
- 范围、单位、上限在输入前显示；错误紧邻字段并给出修复办法。
- 条件编辑使用底部面板，适合单手操作；复杂条件按类型分组并可搜索。
- 每个已启用条件显示状态、参数、单独排除数量和编辑/停用/删除入口。

### 内容状态

统一组件覆盖：

- `Loading`：保留页面骨架或既有数据，局部显示进度。
- `Empty`：说明原因并提供下一步，禁止虚构业务数据。
- `Error`：说明发生了什么、保留了什么、如何重试。
- `Offline`：展示本地数据、最后成功时间和重试入口。
- `Success`：短暂反馈“已保存/已更新”并说明结果去向。
- `Correction`：明确列出受影响期号和“数据已按官方修正更新”。

## 9. 页面与线框

### 9.1 首页

内容优先级：

1. 最新开奖号与期号/日期。
2. 同步状态、最后成功时间、刷新。
3. 和值、跨度、组选形态。
4. “查看走势”“开始选号”两个流程入口。
5. 待复盘与最近方案摘要。
6. 历史查询入口。

状态：

- 本地数据先展示；同步中只更新标题状态。
- 无网/失败用内联状态条，不遮挡开奖号。
- 无开奖数据时显示预置库恢复指引，不展示空的数字卡。

### 9.2 历史查询

- 顶部：最近 `10 / 30 / 50 / 100 / 自定义` FilterChip。
- 查询面板：期号、日期区间、年份三种方式；一次只启用一种主查询方式。
- 列表行：期号、日期、三位号码、和值、跨度、组选形态。
- 点击进入单期属性详情；详情按“基础属性 / 形态 / 两码关系”分组。
- 列表数量与起止期号常驻显示，明确数据起点为 `2017001`。

### 9.3 走势与统计

采用最简单的移动端表达：

- 核心走势图是“期号行 × 0—9列”的位置矩阵，不使用装饰性面积图。
- 百/十/个位可单独显隐；每个命中格显示数字和形状，位置轨迹使用不同线型。
- 横向滚动和 `1x—2.5x` 双指缩放；提供“回到最新”按钮。
- 点击落点打开期号、位置、号码和完整属性底部面板。
- 统计区采用可排序数值表；出现次数/遗漏排行辅以从零开始的水平条。
- 每项同时显示当前遗漏、平均遗漏、最大遗漏、次数和冷/温/热文字。
- 图表提供 TalkBack 摘要：“当前 30 期，百位数字 6 出现 5 次，当前遗漏 2”等文字替代。

加载/错误时保留范围控制；没有足够数据时说明所需与实际期数。

### 9.4 选号

内容顺序：

1. 目标期号、玩法（单选/组选3/组选6）和观察窗口。
2. 已启用条件列表。
3. 添加条件入口。
4. 剩余号码、有效注数、倍数和金额摘要。
5. 惰性候选列表。
6. 保存模板、保存方案。

交互：

- 条件变更立即重算，计算中禁用重复提交。
- 空结果显示已知冲突、完整保留条件并提供“撤销上一步”。
- 倍数限制 `1—99`，金额始终显示“仅用于方案核对，不提供购买”。
- 候选号码始终三位等宽显示；组选结果明确无序注数与转单选排列数。

### 9.5 方案、模板、回测与复盘

- 顶部二选一：方案 / 模板。
- 方案行：期号、标题、玩法、注数/金额、等待开奖或复盘结果。
- 模板行：名称、玩法、观察窗口、条件数、最近修改时间。
- 空方案提供“开始选号”；不展示假方案。
- 详情显示条件、候选、备注、金额、规则版本和复盘，已开奖编辑时创建副本。
- 回测表单先选模板与区间，再显示覆盖期数、未覆盖、样本不足、覆盖率、平均注数、累计金额。
- 逐期回测明确当期使用的历史截止点，不出现未来概率或收益推断。

### 9.6 设置与提醒

- 从首页标题操作进入，不新增第六个一级入口。
- 三个独立开关：新开奖、方案复盘、遗漏阈值。
- 走势图默认周期与显示位置偏好放在同一设置页。
- 开关变化立即保存并反馈；提醒去重状态不向用户暴露内部键。

### 9.7 彩报

- 沿用已批准 `docs/superpowers/specs/2026-07-28-static-caibao-page-design.md`。
- 离线静态只读；没有刷新、翻页、收藏、分享、购买或荐号入口。

## 10. 导航和状态保存

- 五个一级入口各自维护 saveable 状态：滚动位置、筛选周期、已编辑但未保存的条件、方案/模板页签。
- 切换一级入口不重建其 ViewModel。
- 深层页面返回时恢复来源页滚动和筛选条件。
- 同步数据来自 Room Flow；网络响应未写入 Room 前不得更新 UI。
- 界面只接收不可变 `UiState`，一次性反馈通过显式 UI 事件处理。

## 11. 无障碍与窄屏

- 目标宽度优先验证 `320dp / 360dp / 411dp`。
- 所有触控目标至少 `48dp`，相邻目标有可辨识间距。
- TalkBack 顺序按视觉阅读顺序；合并纯装饰节点，保留数字、位置、单位和状态。
- 图表提供文字摘要与可滚动数据表替代。
- 不以红绿、颜色深浅或线条颜色单独传达含义。
- 大字体时工具栏操作允许进入 overflow；卡片改为纵向排列。
- 系统栏采用 edge-to-edge，内容使用安全 inset。

## 12. 反模式

- 不把每段文字包进独立卡片，不嵌套卡片。
- 不使用纯装饰渐变、霓虹、金币、红金庆祝配色或赌场式视觉。
- 不使用“推荐、预测、必中、提高中奖率”等词。
- 不用全屏错误覆盖本地可用数据。
- 不用 Web 的 hover、ARIA、CSS breakpoint 或固定像素方案替代 Compose semantics、窗口 inset 和 dp/sp。
- 不一次渲染全部候选或多年走势图节点；使用 Lazy 容器或 Canvas。

## 13. 验收基线

- 深浅色与大字体截图覆盖首页、走势、选号、方案和彩报。
- 正常、加载、空、错误、无网、重试、成功和数据修正状态都有实现或可验证状态注入。
- API 26 与 API 37 模拟器完成五入口、返回键、无网首启、保存、重启、同步和复盘主流程。
- 走势图使用真实 `data/fc3d-seed.json` 验证 30/100 期密度、缩放、滚动和点选。
- TalkBack 可读出五入口、开奖号、同步状态、筛选条件、候选号码、图表摘要和复盘结果。

## 14. 开放问题

无。真实彩报内容和后台同步属于 V1 之外，不作为本规范待决项。
